import { LoginForm } from '@/components/auth/login-form'

export default function LoginPage() {
  return <LoginForm />
}

export const metadata = {
  title: 'Login - TrendScribe',
  description: 'Sign in to your TrendScribe account',
}